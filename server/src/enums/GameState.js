/**
 * Les états possibles d'une partie.
 *
 * @export
 * @enum {string}
 */
const GameState = {
    Waiting: 'waiting',
    Ready: 'ready',
    Playing: 'playing',
    Finished: 'finished'
};

export default GameState;
